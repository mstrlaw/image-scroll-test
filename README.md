# Gridsome image scroll issue

Simple project to demonstrate issue on g-image when using window.addEventListner('scroll').

Notice when using immediate attribute, image disappears from DOM as user scrolls page.
